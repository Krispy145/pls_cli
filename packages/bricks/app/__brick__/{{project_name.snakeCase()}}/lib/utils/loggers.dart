/// [{{project_name.pascalCase()}}Loggers] is an enum class that contains all the features of the app, used for logging.
enum {{project_name.pascalCase()}}Loggers {
  {{project_name.camelCase()}},
  dependencyInjection,

  ///LOGGER FEATURE END
}
