/// [NameTemplateLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum NameTemplateLoggers {
  /// [nameTemplate] is the feature reference for nameTemplate.
  nameTemplate,
}
