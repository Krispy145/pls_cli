/// [ParentNameTemplateLoggers] class responsible for storing base feature references.
/// Used to determine which features are enabled for logging.
enum ParentNameTemplateLoggers {
  /// [parentNameTemplate] is the feature reference for parentNameTemplate.
  parentNameTemplate,
}
